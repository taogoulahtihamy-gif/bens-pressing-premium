Corrige Tracking.jsx définitivement.

BUG :
La page Suivi appelle encore localhost:5173.
En production, aucun appel localhost ne doit exister.

À faire :
1. Dans Tracking.jsx, supprimer tous les localhost.
2. Créer :
const API_URL = import.meta.env.VITE_API_URL || "https://bens-pressing-premium.onrender.com";

3. Au clic Rechercher, appeler exactement :
fetch(`${API_URL}/api/orders/track/${trackingCode.trim().toUpperCase()}`)

4. Dans server.js, vérifier que cette route existe :
app.get("/api/orders/track/:trackingCode", async (req, res) => {
  const code = req.params.trackingCode.trim().toUpperCase();
  const order = await prisma.order.findUnique({
    where: { trackingCode: code }
  });

  if (!order) {
    return res.status(404).json({ success: false, message: "Commande introuvable" });
  }

  res.json({ success: true, order });
});

5. Après correction :
npm run build
git add .
git commit -m "fix tracking production api url"
git push

6. Ne pas modifier le design.
7. Ne pas casser admin, booking, WhatsApp ou tarifs.