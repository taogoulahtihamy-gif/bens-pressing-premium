Corrige Booking.jsx : en production, ne jamais appeler localhost:5000.
Utiliser uniquement import.meta.env.VITE_API_URL pour l’API backend.

Si VITE_API_URL est absent, utiliser :
https://bens-pressing-premium.onrender.com

Au clic sur Commander :
1. Construire le message WhatsApp.
2. Essayer d’enregistrer la commande via POST `${API_URL}/api/orders`.
3. Même si le backend échoue, ouvrir WhatsApp quand même.
4. Ne jamais bloquer la redirection WhatsApp à cause d’une erreur backend.
5. Corriger les erreurs CORS liées à localhost.
6. Ne pas modifier le design.
7. Ne pas modifier les prix.
8. Ne pas casser le responsive.