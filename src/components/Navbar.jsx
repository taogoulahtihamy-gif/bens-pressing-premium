import React from 'react';
import {useState} from 'react';import {Link,NavLink} from 'react-router-dom';import{Menu,X,CalendarDays}from'lucide-react';
const links=[['/','Accueil'],['/services','Services'],['/pricing','Tarifs'],['/booking','Réservation'],['/tracking','Suivi'],['/contact','Contact']];
export default function Navbar(){const[open,setOpen]=useState(false);return <header className="sticky top-0 z-50 border-b border-gold/10 bg-ink/85 backdrop-blur-xl"><div className="containerx h-20 flex items-center justify-between"><Link to="/" className="flex items-center gap-3"><div className="w-11 h-11 rounded-2xl border border-gold grid place-items-center text-gold font-serif text-xl">B</div><div><p className="font-serif text-2xl font-bold">Ben’s <span className="text-gold">Pressing</span></p><p className="text-[10px] tracking-[.45em] text-gold uppercase">Premium</p></div></Link><nav className="hidden lg:flex items-center gap-8 text-sm font-bold uppercase tracking-wide">{links.map(([to,label])=><NavLink key={to} to={to} className={({isActive})=>isActive?'text-gold':'text-zinc-300 hover:text-gold'}>{label}</NavLink>)}</nav><Link to="/booking" className="hidden lg:inline-flex btn-gold items-center gap-2"><CalendarDays size={17}/> Réserver</Link><button className="lg:hidden text-gold" onClick={()=>setOpen(!open)}>{open?<X/>:<Menu/>}</button></div>{open&&<div className="lg:hidden border-t border-gold/10 bg-ink"><div className="containerx py-5 grid gap-4">{links.map(([to,label])=><Link onClick={()=>setOpen(false)} key={to} to={to} className="py-2 text-zinc-200">{label}</Link>)}<Link onClick={()=>setOpen(false)} to="/booking" className="btn-gold text-center">Réserver maintenant</Link></div></div>}</header>}
